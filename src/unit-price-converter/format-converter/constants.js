'use strict';

const TYPES = {
  WEIGHT : "weight",
  VOLUME : "volume",
  UNITY : "unity"
};

const HEIGHT_UNITS = ['G', 'LB'];
const VOLUME_UNITS = ['L'];
const UNITY_UNITS = ['UN'];

module.exports = {
  TYPES,
  HEIGHT_UNITS,
  VOLUME_UNITS,
  UNITY_UNITS,
};
